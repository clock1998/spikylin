networks:
      tunnel:
        external: true
services:
  spikylin:
    build: 
      context: ../
      dockerfile: ./Spikylin/Dockerfile
    networks:
      - tunnel
    ports:
      - 8080:80
    container_name: spikylin
    restart: always

# docker compose -p services up -d
