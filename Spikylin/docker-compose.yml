networks:
      tunnel:
        external: true
services:
  spikylin:
    build: 
      context: .
      dockerfile: ./Spikylin/Dockerfile
    networks:
      - tunnel
    ports:
      - 7000:8080
    container_name: spikylin.com
    restart: always

# docker compose -p services up -d
